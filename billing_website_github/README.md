# Billing Website

Single-file billing/invoice app (index.html).  
Features:
- Add products, cart, discount, tax.
- Advance payment & auto balance.
- Save invoices locally (by mobile / invoice no).
- Print invoice preview (Print -> Save as PDF).

To publish on GitHub Pages:
1. Create a GitHub repository (public).
2. Upload the files (index.html, README.md).
3. In repo Settings → Pages, set branch `main` and root `/`.
4. Your site will be available at https://<username>.github.io/<repo-name>/

Or use the provided `git` commands below.

